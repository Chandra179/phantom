/// Computes period-to-period percentage changes.
pub fn percent_changes(_prices: &[f64]) -> Vec<f64> {
    unimplemented!()
}

/// Builds a price window array around T0.
pub fn build_window(
    _all_prices: &[f64],
    _t0_index: usize,
    _est_days: u32,
    _event_hours: u32,
) -> Vec<f64> {
    unimplemented!()
}
